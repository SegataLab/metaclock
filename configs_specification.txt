[
  {
    "input_type":"reads", # Fixed
    "reference_genome":"", # Required, but user can choose to input as commmand line or in the config file
    "age_type":1, # Fixed
    "intermediate":"", # Required, but user can choose to input as command line or in the config file
    "parameter_set":{
      "search_report_mode":"-k,1", # optional 
      "bowtie2_threads":15, optional 
      "minimum_mapping_quality":30, # optional
      "minimum_mapping_length":30, # optional 
      "maximum_snp_edit_distance":0.04, # optional 
      "nproc":3, optional # optional
      "minimum_coverage":30, # optional 
      "trim_distance":"5:5", # optional 
      "dominant_allele_frequency":0.8, # optional
      "output_trimmed_reads":1, # optional
      "samples":"" # conditionally optional 
    }
  },
  {
    "input_type":"reads", # Fixed
    "reference_genome":"", # Required, but user can choose to input as commmand line or in the config file
    "age_type":2, # Fixed
    "intermediate":"",
    "parameter_set":{
      "search_report_mode":"-k,1", # optional 
      "bowtie2_threads":15, # optional 
      "minimum_mapping_quality":30, # optional 
      "minimum_mapping_length":30, # optional 
      "maximum_snp_edit_distance":0.04, # optional 
      "nproc":3, # optional 
      "minimum_coverage":30, # optional 
      "dominant_allele_frequency":0.8, # optional 
      "samples":"" # conditionally optional 
    }
  },
  {
    "input_type":"contigs", # Fixed
    "reference_genome":"", # Required, but user can choose to input as commmand line or in the config file
    "intermediate":"", # Fixed
    "parameter_set":{
      "homolog_length":500, # optional
      "homolog_identity":95, # optional
      "blastn_threads":10, # optional
      "samples":"" # conditionally optional
    }
  }
]

#conditionally optional: at least one of three "samples" but not each of them have to be given
